import * as React from 'react';
import Layout from '../components/layout';
import TeamsComponent from '../components/teams';

const Teams = () => {
    return <Layout pageTitle="Kontakt">
        <TeamsComponent></TeamsComponent>
    </Layout>
}

export default Teams
