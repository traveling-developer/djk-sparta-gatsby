import { createTheme } from '@mui/material';

const theme = createTheme({
    palette: {
        primary: {
            main: '#d50000',
        },
        secondary: {
            main: '#00bcd4',
        }
    },
});

export default theme;
